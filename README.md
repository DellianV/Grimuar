# Гримуар SRD (RU UI) — Netlify “под ключ”

Полный список заклинаний **SRD 5.1** подтягивается автоматически на этапе билда (Open5e API), 
UI на русском, офлайн-PWA, фильтры, автотеги, избранное, импорт/экспорт, DM Presets.

## Быстрый старт локально
```bash
npm ci
npm run prebuild   # скачает полный SRD и создаст data/spells.json
python -m http.server 8080  # или npx serve
```

Открой http://localhost:8080

## Деплой на Netlify
1. Создай новый сайт из этого репозитория/архива.
2. Build command: `npm ci && npm run prebuild`
3. Publish directory: `./`
4. Готово. Первый билд подтянет весь SRD и положит `data/spells.json`. 
   Service Worker закеширует и будет работать офлайн.

## Правовой статус
Содержимое SRD 5.1 — CC BY 4.0. Источник данных: Open5e (SRD). 
Текст описаний в приложении — **краткие сводки** на русском, не являются переводом правил.

## Импорт/экспорт
- Кнопки «Импорт/Экспорт» в шапке — для своих домашних спеллов/пакетов.
- Формат совпадает с `data/spells.json`.

## RU имена
По умолчанию — оригинальные EN названия (из SRD). При желании можно подключить карту русских названий — добавь `data/ru-map.json` вида:
```json
{"Magic Missile": "Волшебная стрела", "Shield": "Щит", "...": "..."}
```
и допиши автозамену в `app.js` (hook после загрузки).

Удачных бросков! 🎲
